import ClientIdeasLayout from './ClientIdeasLayout';

export default function IdeasLayout({ 
  children 
}: { 
  children: React.ReactNode 
}) {
  return <ClientIdeasLayout>{children}</ClientIdeasLayout>;
}